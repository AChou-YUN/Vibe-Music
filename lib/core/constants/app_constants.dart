class AppConstants {
  static const appName = 'Vibe Music';
  static const appVersion = '1.0.0';

  static const supportedExtensions = ['mp3', 'flac', 'wav', 'ape', 'ogg', 'aac', 'm4a', 'wma'];

  static const miniPlayerWidth = 320.0;
  static const miniPlayerHeight = 90.0;
  static const playerBarHeight = 72.0;
  static const sideBarWidth = 200.0;
  static const titleBarHeight = 32.0;

  static const animationDurationFast = Duration(milliseconds: 150);
  static const animationDurationNormal = Duration(milliseconds: 300);
  static const animationDurationSlow = Duration(milliseconds: 500);
}
